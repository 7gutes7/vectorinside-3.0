{
  "name": "vectorinside-3.0",
  "version": "3.0.0",
  "scripts": {
    "dev": "live-server --port=8080 --watch=. --wait=200",
    "start": "live-server --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "hls.js": "^1.7.0"
  }
}
